name: combat_goblin_prime
description: Deterministic multi-phase data pipeline engine for structured game system processing (M1-M9+ modules)
version: 0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  fake_async: ^1.3.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audio_session: ^0.1.22
  crypto: ^3.0.3
  file_picker: ^8.0.0
  http: ^1.2.2
  path: ^1.9.0
  path_provider: ^2.1.2
  permission_handler: ^11.0.0
  record: ^5.0.0
  sherpa_onnx: ^1.10.0
  xml: ^6.5.0

flutter:
  uses-material-design: true
  assets:
    - assets/voice/kws/
    - assets/voice/asr/
